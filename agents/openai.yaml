interface:
  display_name: "Paper Summary"
  short_description: "Korean paper review with embedded figures"
  default_prompt: "Use $paper-summary to produce a Korean critical review with PDF figure captures embedded directly in Markdown."
